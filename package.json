{
  "name": "yandex-cloud-terraform",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "./scripts/build.sh && cd terraform && terraform apply -auto-approve"
  },
  "devDependencies": {}
}
